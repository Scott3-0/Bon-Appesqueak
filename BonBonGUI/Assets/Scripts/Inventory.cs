using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
//Useful tutorial & resources and shiz under the "UI_Inventory.cs" script
public class Inventory //Generic class  for inventory 
{
    private List<Item> itemList;

    public Inventory()
    {
        itemList = new List<Item>(); //creates new list of the item type applicable to this inventory

        AddItem(new Item { itemType = Item.ItemType.Bread, amount = 1}); //For debug and shiz

        Debug.Log(itemList.Count); //For debug and shiz
        Debug.Log("Inventory"); //For debug and shiz
    }

    public void AddItem(Item item) //adds the items to this local inventory
    {
        itemList.Add(item);
    }

    public List<Item> GetItemList() //gets item list data
    {
        return itemList;
    }
}
*/